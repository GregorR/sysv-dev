diff --git a/configure b/configure
--- a/configure
+++ b/configure
@@ -2059,6 +2059,14 @@
 	esac
 	;;
 
+    sysv4*)
+        targos=sysv4
+        abi=elf
+        case "$target_cpu" in
+            i?86) targmach=i386 ;;
+        esac
+        ;;
+
     sunos*|solaris*)
         targos=sunos
         abi=elf
diff --git a/configure.ac b/configure.ac
--- a/configure.ac
+++ b/configure.ac
@@ -169,6 +169,14 @@
 	esac
 	;;
 
+    sysv4*)
+        targos=sysv4
+        abi=elf
+        case "$target_cpu" in
+            i?86) targmach=i386 ;;
+        esac
+        ;;
+
     sunos*|solaris*)
         targos=sunos
         abi=elf
diff --git a/mip/compat.h b/mip/compat.h
--- a/mip/compat.h
+++ b/mip/compat.h
@@ -7,6 +7,7 @@
 #define COMPAT_H
 
 #include <string.h>
+#include <stdarg.h>
 
 #ifndef HAVE_STRLCPY
 size_t strlcpy(char *dst, const char *src, size_t siz);
diff --git a/os/sunos/ccconfig.h b/os/sysv4/ccconfig.h
copy from os/sunos/ccconfig.h
copy to os/sysv4/ccconfig.h
--- a/os/sunos/ccconfig.h
+++ b/os/sysv4/ccconfig.h
@@ -1,7 +1,6 @@
-/*	$Id: ccconfig.h,v 1.3 2009/05/19 05:15:38 gmcgarry Exp $	*/
-
 /*
  * Copyright (c) 2008 Adam Hoka.
+ * Modified from sunos files slightly by Gregor Richards.
  * All rights reserved.
  *
  * Redistribution and use in source and binary forms, with or without
@@ -36,7 +35,7 @@
 #endif
 
 /* common cpp predefines */
-#define	CPPADD	{ "-Dunix", "-Dsun", "-D__SVR4", "-D__unix", "-D__sun", "-D__SunOS", "-D__ELF__", NULL }
+#define	CPPADD	{ "-Dunix", "-D__SVR4", "-D__unix", "-D__svr4__", "-D__ELF__", NULL }
 
 /* TODO: _ _SunOS_5_6, _ _SunOS_5_7, _ _SunOS_5_8, _ _SunOS_5_9, _ _SunOS_5_10 */
 
@@ -54,16 +53,11 @@
 #define F77LIBLIST { "-L/usr/local/lib", "-lF77", "-lI77", "-lm", "-lc", NULL };
 #endif
 
-/* host-independent */
-#define	DYNLINKER { "-Bdynamic", "/usr/lib/ld.so", NULL }
+/* assumes GNU ld */
+#define	DYNLINKER { "--dynamic-linker", "/usr/lib/ld.so.1", NULL }
 
 #if defined(mach_i386)
 #define	CPPMDADD { "-D__i386__", "-D__i386", NULL, }
-/* Let's keep it here in case of Polaris. ;) */
-#elif defined(mach_powerpc)
-#define	CPPMDADD { "-D__ppc__", NULL, }
-#elif defined(mach_sparc64)
-#define CPPMDADD { "-D__sparc64__", "-D__sparc", NULL, }
 #else
 #error defines for arch missing
 #endif
